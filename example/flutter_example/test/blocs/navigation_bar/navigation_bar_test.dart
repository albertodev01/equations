import 'package:bloc_test/bloc_test.dart';
import 'package:equations_solver/blocs/navigation_bar/navigation_bar.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  group("Testing the 'NavigationBloc' bloc", () {
    /*test('Making sure that the initial state of the bloc is zero', () {
      final bloc = NavigationBloc();

      expect(bloc.state, isZero);
    });

    blocTest<NavigationBloc, int>(
      'Making sure that the bloc emits states',
      build: () => NavigationBloc(),
      act: (bloc) => bloc..add(1)..add(10),
      expect: () => const [1, 10],
    );

    blocTest<NavigationBloc, int>(
      'Making sure that the bloc skips negative values',
      build: () => NavigationBloc(),
      act: (bloc) => bloc..add(1)..add(-2)..add(3),
      expect: () => const [1, 3],
    );*/
  });
}
