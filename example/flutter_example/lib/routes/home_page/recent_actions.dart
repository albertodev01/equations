import 'package:flutter/material.dart';

/// A list containing the latest 5 actions performed by the user.
class RecentActions extends StatelessWidget {
  const RecentActions();

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
