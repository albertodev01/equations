/// Represents the types of polynomials that a bloc has to to handle
enum PolynomialType {
  /// A polynomial whose degree is 1
  linear,

  /// A polynomial whose degree is 2
  quadratic,

  /// A polynomial whose degree is 3
  cubic,

  /// A polynomial whose degree is 4
  quartic,
}