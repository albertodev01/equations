/// Path to the polynomial equations route.
const homeRoute = '/';

/// Path to the nonlinear equations route.
const nonlinearRoute = '/nonlinear';

/// Path to the system solving equations route.
const systemsRoute = '/systems';

/// Path to the integral evaluators route.
const integralsRoute = '/integrals';

/// Path to the complex numbers analyzer route.
const complexNumbersRoute = '/complex-numbers';

/// Path to the interpolation nodes calculator route.
const interpolationRoute = '/interpolation';

/// Path to the matrices analyzer route.
const matricesRoute = '/matrices';

/// Path to the generic error route.
const errorRoute = '/error';
