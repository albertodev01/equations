import 'package:flutter/material.dart';

class NonlinearView extends StatelessWidget {
  const NonlinearView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('nonlinear'),
    );
  }
}
